package com.example.askfm.repository;

import com.example.askfm.model.TestPhoto;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TestPhotoRepository extends JpaRepository<TestPhoto, Long> {
}
